import SectionTitle from "../shared/SectionTitle";

const Sponsors = () => {
    return (
        <section>
            <SectionTitle title="Sponsors"/>
        </section>
    )
}

export default Sponsors;